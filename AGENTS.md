# AGENTS.md

## プロジェクト

MachiVerse は、C# で開発する超大規模エージェントベースの世界シミュレーターです。

## 作業ルール

1. ユーザーから明示的な実装指示がない限り、コーディング、ソースファイル編集、実装ファイル作成、その他の実装変更を開始しないこと。
2. 議論、要件整理、アーキテクチャ設計、調査、見積もり、計画は、実装が明示的に依頼されない限り実装作業と分離すること。
3. ユーザーが指定していないシミュレーション要件を勝手に推測して確定事項として扱わないこと。ユーザーから提示された要件と、提案・仮定を明確に区別すること。
4. MachiVerse はエージェントベースの世界シミュレーターを採用する。ただし、具体的なエージェント種別、挙動、世界を構成する対象、規模目標、性能要件、シミュレーションルールは、明示された要件に基づいて定義するか、提案として明確に提示すること。
5. 世界シミュレーターの要件定義・設計における第一の上位原則として、常に「どうすれば狂気的なまでに世界をシミュレーションできるか」を判断軸として考えること。単なる機能数や表面的な再現ではなく、世界を構成する要素、状態、因果、相互作用、時間的変化がシステムとして成立する方向を優先して検討すること。ただし、これは物理エンジンや物理演算の高精度化を主目的とする指示ではなく、具体的な世界要素やモデルを未承認のまま確定する根拠にもしてはならない。
6. 世界シミュレーターの要件定義・設計における第二の上位原則として、利用者ロール「ダイバー」が「自分は世界を構成する一人の住人である」と感じられる体験を最大化すること。ここでいう「フルダイブ」はその体験を表す比喩であり、VR、身体トラッキング、写実的レンダリング等の技術方式そのものを意味しない。ダイバーを世界の外から操作するクライアントではなく、世界のルール・因果・時間・社会的関係・周囲の反応等の中に存在する一住民として扱える方向を優先して検討すること。画面の見た目や表現品質は重要な利用者体験要素として扱うが、それ自体を第二原則の本質と混同しないこと。VR等は将来的なアドオン候補として扱えるが、この原則だけを根拠に特定の提示・入力技術を必須要件として確定してはならない。
7. 世界シミュレーターの要件定義・設計における第三の上位原則として、世界を現在時点の状態だけで成立させず、歴史的因果関係を持つ動的な過程として考えること。都市・集落その他の世界要素は固定された完成物として配置するのではなく、過去の状態、住民の需要、自然環境その他の要因と相互作用しながら成立・成長・変化・衰退・回復し得るものとして検討すること。例えば、何もない場所が集落となり、村、町、市、大都市へ発展することも、条件の変化によって衰退することも、再び成長へ転じることも、いずれも歴史の結果として生じ得る設計を志向すること。特定の発展段階や一方向の成長を必然として固定せず、「現在の状態が過去の何によって形成されたか」「現在の変化が将来へ何を残すか」を常に検討すること。ただし、この原則の具体例だけを根拠に都市分類、発展条件、需要モデル、自然環境モデル等の詳細要件を未承認のまま確定してはならない。
8. 世界シミュレーターの要件定義・設計における第四の上位原則として、世界を単一都市または都市だけで完結する系として考えないこと。複数の都市・集落・その他の居住地点が存在し、それらの間の需要や相互作用によって世界が成立・変化し得るものとして検討すること。既存都市だけを前提にせず、需要その他の世界内部の条件によって、それまで何もなかった場所に新たな集落が生まれる可能性も考慮すること。また、住民の居住を都市・集落への所属に限定せず、個人の選択等によって一人または少人数が山奥などの非都市地域に居住することも成立し得る世界として考えること。都市・集落は世界を構成する重要な要素ではあるが、住民存在の必須コンテナとして扱わないこと。ただし、この原則だけを根拠に都市数、集落生成条件、都市間経済モデル、居住選択モデル等の詳細要件を未承認のまま確定してはならない。
9. 世界シミュレーターの要件定義・設計における第五の上位原則として、人の営みは自然環境から強い影響を受けるものとして考えること。地形、水域、植生その他の自然条件を単なる背景や装飾として扱わず、居住、都市形成、生産、産業、交通その他の人間活動が成立・発展・変化する条件の一部として検討すること。例えば、平地が都市形成や農地利用の可能性を生み、海が海運発展の可能性を生み、山や森林が林業発展の可能性を生み、豊かな自然環境が観光業発展の可能性を生むように、土地・自然の性質が人の選択と需要を通じて異なる営みへ結び付く世界を志向すること。ただし、自然条件から特定の結果が機械的・必然的に発生すると固定せず、あくまで住民の需要、選択、歴史、他の世界要素との相互作用を通じて結果が生じ得るものとして扱うこと。また、この原則だけを根拠に具体的な地形分類、資源分類、産業分類、適性値、発展条件等の詳細要件を未承認のまま確定してはならない。
10. 世界シミュレーターの要件定義・設計における第六の上位原則として、詳細に常時シミュレーションする有限の対象領域だけを自然環境上の閉じた世界として扱わないこと。対象領域の自然環境は、その境界の外側に存在する環境からも影響を受け得るものとして設計すること。シミュレーション外エリアについて対象領域と同等の詳細度・更新頻度で常時シミュレーションする必要はないが、定期的に簡易シミュレーションを行い、その結果から対象領域へ及ぶ影響を生成・反映すること。これにより、対象領域の境界を越えて成立する自然現象や環境上の因果関係を、境界で不自然に切断しないこと。簡易シミュレーションも同一Seed・同一設定・同一操作等に基づく決定性要件を破ってはならない。具体的な対象領域の範囲、外部エリアの範囲、簡易化手法、更新周期、境界条件、対象領域へ伝播させる自然要素等は別途要件として定義し、この原則だけを根拠に未承認の詳細を確定してはならない。
11. リポジトリへ変更を加える前に、その変更自体がユーザーから許可された範囲に含まれることを確認すること。プロジェクト全体の目的を、無関係な実装変更の許可として解釈しないこと。
12. 今後追加されるプロジェクト固有の運用ルールは、この AGENTS.md に追記し、後続の開発作業から参照できる状態にすること。
13. 実行頻度、時間間隔、遅延時間、タイムアウト、件数・サイズ上限、再試行回数、キャッシュ・バッファ・流量制御・表示更新等、運用・性能・シミュレーション挙動を調整する各種数値は、原則としてソースコードへ固定せず、各コンポーネントが所有する外部Configから変更可能にすること。設定値として意味を持たない数学的定数や実装上の数値は対象外とする。
14. すべての要件定義・アーキテクチャ設計・コンポーネント設計では、将来的なアドオン対応を見据え、標準実装を不要に複雑化せずに機能や振る舞いを追加・差し替え・拡張できる余地を残すこと。標準実装の内部詳細へアドオンが密結合しなければ成立しない構造や、将来拡張のために既存のコンポーネント境界・プロトコル契約を破壊しなければならない構造を可能な限り避けること。ただし、この原則だけを根拠に具体的なアドオンAPI、プラグイン方式、ロード方式、拡張ポイント、互換性保証範囲、サンドボックス方式等を未承認のまま確定せず、現在の標準要件・性能・決定論・コンポーネント独立性を犠牲にする過剰な抽象化も行わないこと。
15. 対話ベースの要件定義では、関連する質疑を可能な範囲で数件まとめ、各質問に複数の具体的な選択肢を提示すること。選択肢は検討を助けるための案であり、ユーザーは選択肢にないカスタム指定を個別に指示できるものとして扱うこと。選択肢の提示だけを根拠に未選択の内容を確定事項として扱わないこと。

## ブランチ運用ルール

### 常設ブランチ

以下のブランチを常設ブランチとして扱う。

- `main`: 安定版・リリース可能な状態を保持する。通常の実装作業を直接行わない。
- `develop`: 全コンポーネントおよび共通ドキュメントを統合した次期版を保持する。通常の機能実装や修正を直接行わない。
- `simulation`: シミュレーションコアの統合ブランチ。
- `gateway`: ゲートウェイの統合ブランチ。
- `view`: 一般ビューの統合ブランチ。
- `administration-view`: 管理ビューの統合ブランチ。
- `documentation`: リポジトリ共通のドキュメント・設計文書・Protocol文書の統合ブランチ。

ブランチ名は原則として小文字を使用し、複数語はハイフンで区切る。

### 通常の実装・修正フロー

1. 機能追加、修正、リファクタリング等の作業は、対象コンポーネントの常設ブランチから作業ブランチを作成して行う。
2. 作業ブランチで完了した変更は、対象コンポーネントの常設ブランチへ Pull Request で統合する。
3. コンポーネント単位で `develop` へ統合可能な状態になった変更は、対象コンポーネントブランチから `develop` へ Pull Request で統合する。
4. リポジトリ共通のドキュメント変更は `documentation` から作業ブランチを作成して行い、作業ブランチから `documentation`、`documentation` から `develop` の順に Pull Request で統合する。
5. `develop` がリリース可能な状態になった場合、`develop` から `main` へ Pull Request で統合する。
6. `main`、`develop`、各コンポーネント常設ブランチ、`documentation` 上で、通常の機能実装・修正・ドキュメント編集を直接コミットしないこと。

基本的な流れは以下とする。

```text
main
  ↑
develop
  ↑
responsibility branch
  ↑
work branch
```

ここで `responsibility branch` は、対象に応じて `simulation`、`gateway`、`view`、`administration-view`、`documentation` のいずれかを指す。

### ドキュメント編集フロー

1. `README`、`CONTRIBUTING.md`、`SUPPORT.md`、`docs/architecture/`、`docs/protocols/` その他のリポジトリ共通ドキュメントは、原則として `documentation` の責任分野として扱う。
2. 共通ドキュメントを編集する場合は `documentation` から `docs/` 接頭辞の作業ブランチを作成し、Pull Request で `documentation` へ統合する。
3. `documentation` 上で統合・レビュー可能な状態になった変更は、`documentation` から `develop` へ Pull Request で統合する。
4. 各コンポーネント固有の実装コードと不可分なドキュメントがある場合でも、責任分野をまたぐ修正を同一作業ブランチへ無断で含めず、必要に応じて別Issue・別Pull Requestへ分離する。
5. `.github/workflows/` はドキュメントではなくリポジトリ共通のCI / Workflow資産として扱い、後述のCI / Workflow運用ルールを優先する。

### 別責任分野への修正依頼

1. 作業中に、現在の作業ブランチとは別の常設ブランチが責任を持つファイル・設計・実装の修正が必要になった場合、原則として現在の作業ブランチからその責任分野へ直接変更を加えないこと。
2. 別責任分野の修正が必要な場合は GitHub Issue を作成し、修正が必要な理由、期待する変更、影響範囲、関連する設計文書・Protocol・Issue・Pull Request等を記載して、対象責任分野へ依頼すること。
3. 依頼された修正は、対象責任分野の常設ブランチから独立した作業ブランチを作成して対応し、対象の常設ブランチへ Pull Request で統合すること。
4. 1つの要求が複数の責任分野へ影響する場合も、Issueを連携点として各責任分野の作業を分離し、複数責任分野の実装や文書を1つの作業ブランチへ無断でまとめないこと。
5. Protocolや正本となる設計文書の変更が必要な場合は、実装側で先に契約を変更せず、Issueを通して `documentation` 側へ変更を依頼し、正本の更新が `develop` へ統合された後に各コンポーネント側で追従すること。
6. 緊急対応その他の理由で責任分野をまたぐ変更を1つの作業として扱う必要がある場合は、例外として黙示的に実施せず、事前にユーザーから明示的な承認を得ること。

### 作業ブランチ命名

作業ブランチは、目的が判別できる接頭辞を付ける。

例:

- `feature/simulation-world-time`
- `fix/gateway-master-reselection`
- `refactor/simulation-threading`
- `docs/core-concurrency`

接頭辞や名称は作業内容を明確に表現し、必要以上にコンポーネントや責任分野をまたぐ変更を1ブランチへ含めない。

### プロトコル変更

コンポーネント間プロトコルを変更する場合は、実装変更より先にプロトコル設計書を更新する。

1. Protocol変更の必要性を確認した責任分野は、変更理由、影響範囲、必要な契約変更をIssueとして `documentation` 側へ依頼する。
2. `documentation` を基準として、プロトコル変更専用の `docs/` 作業ブランチを作成する。
3. `docs/protocols` 配下の該当設計書を先に変更し、Pull Requestで `documentation` へ統合する。
4. Protocol変更を `documentation` から `develop` へ Pull Request で統合する。
5. 各コンポーネントは、それぞれのコンポーネントブランチから独立した作業ブランチを作成し、`develop` へ統合された更新済みプロトコル設計書へ追従する。
6. プロトコル変更を理由に、複数コンポーネントの実装コードを1つの共有作業ブランチへまとめない。

例:

```text
component responsibility
  ↓ Issue
documentation
  ↓
docs/core-gateway-v1.1
  ↓ PR
documentation
  ↓ PR
develop

simulation
  ↓
feature/support-core-gateway-v1.1

gateway
  ↓
feature/support-core-gateway-v1.1
```

プロトコル設計書をコンポーネント間契約の正本とし、利用側コンポーネントは相手コンポーネントの実装ブランチや内部コードを通信契約として参照しない。

### コンポーネントブランチと develop の同期

各コンポーネントブランチを `develop` から長期間乖離させないこと。

他コンポーネント、プロトコル、共通ドキュメント等の変更を取り込む必要がある場合は、対象コンポーネントの作業と矛盾しないタイミングで `develop` の変更をコンポーネントブランチへ取り込み、統合時に大規模な差分・競合を一度に発生させないようにする。

具体的な同期頻度や merge / rebase の採用方針は現時点では固定しない。

### documentation と develop の同期

`documentation` を `develop` から長期間乖離させないこと。

コンポーネント統合やリポジトリ共通運用の変更によりドキュメントの前提が更新された場合は、ドキュメント作業と矛盾しないタイミングで `develop` の変更を `documentation` へ取り込み、正本となる文書が古い統合状態を前提にし続けないようにする。

具体的な同期頻度や merge / rebase の採用方針は現時点では固定しない。

### ブランチ境界とコンポーネント独立性

ブランチ運用でもコンポーネント完全分離の原則を維持する。

- `simulation` の実装変更を `gateway` の実装変更と同一作業として必須にしない。
- `gateway`、`view`、`administration-view` についても同様とする。
- 複数コンポーネント間の契約変更はプロトコル設計書を介して連携する。
- 並行開発時に各コンポーネントが同一実装ファイルを編集しなければならない構造を作らない。
- コンポーネント側から `documentation` の責任分野を直接編集すること、および `documentation` 側から各コンポーネントの実装責任分野を直接編集することを通常フローにしない。

## CI / Workflow 運用ルール

### 基本方針

1. GitHub Actions の Workflow、CI、リポジトリ横断の自動化は、コンポーネント実装ではなくリポジトリ共通の運用資産として扱う。
2. Workflow は原則として `.github/workflows/` 配下で管理し、内容をコードレビュー可能な状態でリポジトリへ保持する。
3. CI は品質を検証するための仕組みであり、CIを通すためだけに本来のコンポーネント境界、プロトコル契約、決定論要件、設計原則を崩してはならない。
4. 実装対象、ビルド方法、テスト方法等が未確定の状態で、仮定に基づくCIやWorkflowを先回りで作成しない。

### Workflow変更用ブランチ

1. リポジトリ共通のCI / Workflowを追加・変更・削除する場合は、原則として `develop` から専用作業ブランチを作成する。
2. Workflow用作業ブランチは `ci/` 接頭辞を使用する。
3. Workflow変更は作業ブランチから `develop` への Pull Request で統合する。
4. `main`、`develop`、`documentation`、各コンポーネント常設ブランチへWorkflow変更を直接コミットしない。
5. 特定コンポーネントだけを対象とするCIであっても、`.github/workflows` がリポジトリ共通領域である以上、Workflow定義そのものは `develop` 基準の `ci/` ブランチで管理する。

例:

```text
develop
  ↓
ci/simulation-build
  ↓ PR
develop
```

### コンポーネント独立性

1. Simulation、Gateway、View、AdministrationView のビルド・テストは、可能な限りコンポーネントごとに独立したCI単位として扱う。
2. あるコンポーネントのCIを実行するために、別コンポーネントの実装コード、DLL、内部型、共有DTOライブラリを必要とする構成を作らない。
3. コンポーネント間通信のテストでは、相手コンポーネントの実装そのものではなく、プロトコル設計書に基づくスタブ、モック、テストPeer等で独立検証できる方向を優先する。
4. 変更対象と無関係なコンポーネントの高コストなCIを無条件に実行しない。具体的な path filter、依存判定、実行条件は実装構成が確定した時点で定義する。
5. リポジトリ全体の統合検証が必要な場合は、各コンポーネント単体CIとは別の統合Workflowとして責務を明示する。

### Pull Request と必須チェック

1. Pull Request向けCIは、原則としてビルド、テスト、静的検査等の検証処理に限定し、デプロイ、リリース、外部環境の破壊的変更を行わない。
2. Branch Rules の `Require status checks to pass` に登録するチェックは、実際に存在し、安定して実行でき、対象ブランチの統合可否を判断するうえで必要なものだけとする。
3. 存在しないCI、未完成のCI、不安定なCIを先に必須チェックへ登録しない。
4. CI障害とコード不具合を区別できるよう、WorkflowおよびJobの名称は目的と対象コンポーネントが判別できる名称にする。
5. CIを一時的に無効化してマージ制約を回避する変更は、通常の修正手段として扱わない。

### 権限とセキュリティ

1. Workflowの `GITHUB_TOKEN` 権限は最小権限を原則とし、必要な権限だけを `permissions` で明示する。
2. 書き込み権限を必要としない検証Workflowへ、リポジトリ内容、Pull Request、Issues、Packages等への不要なwrite権限を付与しない。
3. トークン、APIキー、証明書、パスワードその他の秘密情報をWorkflowファイル、ソースコード、ログ、Artifactへ直接記録しない。
4. 秘密情報が必要な場合は GitHub Secrets または適切なEnvironment等の秘密情報管理機構を使用する。具体的なSecret構成は必要性が確定した時点で定義する。
5. Fork由来のPull Requestや信頼できない入力から、秘密情報へ到達できるWorkflowを作らない。
6. `pull_request_target` のようにベースブランチ側の権限・Secretsへ到達し得るトリガーは、明確な必要性とセキュリティ設計なしに使用しない。
7. PRタイトル、ブランチ名、Issue本文、コミットメッセージ等の外部入力をShellコマンドへ直接展開しない。

### 外部Actionと依存関係

1. 外部Actionを導入する場合は、提供元、用途、必要権限を確認する。
2. GitHub Actionsで参照する外部Actionは、変更可能なブランチ名だけに依存せず、再現可能性とサプライチェーン安全性を考慮して不変なコミットSHAへ固定することを原則とする。
3. 外部Actionのバージョン更新は通常の依存関係更新としてレビュー対象にする。
4. 不要なActionを追加せず、標準Shellや既存の信頼できるActionで明確に実現できる処理へ過剰な外部依存を導入しない。

### デプロイ・リリースWorkflow

1. デプロイ、Release作成、Package公開、外部サービス更新等、リポジトリ外へ副作用を与えるWorkflowは、通常のPR検証Workflowと分離する。
2. デプロイ・リリースWorkflowは、その対象、実行元ブランチ、手動承認、Environment、必要権限、失敗時の扱いが明示的に設計されるまで作成しない。
3. `main` への統合だけを根拠として、未承認のデプロイ先へ自動配布する構成を勝手に追加しない。
4. 破壊的操作または公開操作を行うWorkflowには、誤実行を防ぐための明示的な実行条件を設ける。

### Workflow品質

1. Workflow内の処理は、同じ入力に対して可能な限り再現可能であることを優先する。
2. Jobの責務を明確にし、ビルド、テスト、静的解析、パッケージ作成、デプロイ等の異なる責務を理由なく1つの巨大なJobへ混在させない。
3. Artifactを生成する場合は、目的、保持対象、秘密情報を含まないことを確認する。保持期間等の具体値は必要性に応じて設定する。
4. キャッシュはCI高速化のために利用できるが、キャッシュの有無によって検証結果の正否が変わる構成にしない。
5. CI時間短縮だけを理由に必要な検証を無断で省略しない。検証範囲を縮小する場合は、その条件を明示する。

## 言語ルール

1. このリポジトリは日本語を基準言語とする。
2. README、設計書、仕様書、運用手順、開発ガイド、ADR、Issue テンプレート、Pull Request テンプレートなど、ドキュメント関係は原則として日本語で作成すること。
3. ドキュメント内の説明文、見出し、注釈も原則として日本語とする。
4. 技術用語、API 名、クラス名、メソッド名、ライブラリ名、規格名など、英語表記が自然または必要なものは無理に日本語化しないこと。
5. 英語版ドキュメントを作成する場合は、ユーザーから明示的な指示がある場合に限る。

## 優先順位

提案または実行しようとしている作業がこのルールと競合する場合は、実装前に停止し、ユーザーから明示的な実装指示または方針変更があるまで、議論・要件整理・計画に戻ること。

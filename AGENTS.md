# AGENTS.md

## プロジェクト

MachiVerse は、C# で開発する超大規模エージェントベースの世界シミュレーターです。

## 作業ルール

1. ユーザーから明示的な実装指示がない限り、コーディング、ソースファイル編集、実装ファイル作成、その他の実装変更を開始しないこと。
2. 議論、要件整理、アーキテクチャ設計、調査、見積もり、計画は、実装が明示的に依頼されない限り実装作業と分離すること。
3. ユーザーが指定していないシミュレーション要件を勝手に推測して確定事項として扱わないこと。ユーザーから提示された要件と、提案・仮定を明確に区別すること。
4. MachiVerse はエージェントベースの世界シミュレーターを採用する。ただし、具体的なエージェント種別、挙動、世界を構成する対象、規模目標、性能要件、シミュレーションルールは、明示された要件に基づいて定義するか、提案として明確に提示すること。
5. 世界シミュレーターの要件定義・設計における第一の上位原則として、常に「どうすれば狂気的なまでに世界をシミュレーションできるか」を判断軸として考えること。単なる機能数や表面的な再現ではなく、世界を構成する要素、状態、因果、相互作用、時間的変化がシステムとして成立する方向を優先して検討すること。ただし、具体的な世界要素やモデルを未承認のまま確定する根拠にはしない。
6. 第二の上位原則として、利用者ロール「ダイバー」が「自分は世界を構成する一人の住人である」と感じられる体験を最大化すること。「フルダイブ」はその体験を表す比喩であり、VR等の特定技術を必須化する根拠にはしない。
7. 第三の上位原則として、世界を現在状態だけでなく歴史的因果関係を持つ動的過程として考えること。世界要素は成立・成長・変化・衰退・回復し得るものとして扱い、「現在が過去の何によって形成されたか」「現在の変化が将来へ何を残すか」を検討すること。具体的モデルを未承認のまま確定しない。
8. 第四の上位原則として、世界を単一都市または都市だけで完結する系として考えないこと。複数の都市・集落・その他の居住地点、非都市地域での居住、新規集落成立の可能性を含めて検討すること。具体的な都市数や生成条件等を未承認のまま確定しない。
9. 第五の上位原則として、人の営みは自然環境から強い影響を受けるものとして考えること。地形、水域、植生その他の自然条件を背景ではなく、人間活動が成立・変化する条件として扱うこと。ただし特定結果を機械的に固定せず、具体的分類や条件を未承認のまま確定しない。
10. 第六の上位原則として、詳細シミュレーション対象領域だけを自然環境上の閉じた世界として扱わないこと。外部領域は簡易シミュレーション等により対象領域へ影響し得るものとし、決定性要件を維持すること。具体的範囲・手法・周期・境界条件等を未承認のまま確定しない。
11. リポジトリへ変更を加える前に、その変更自体がユーザーから許可された範囲に含まれることを確認すること。プロジェクト全体の目的を無関係な実装変更の許可として解釈しないこと。
12. 今後追加されるプロジェクト固有の運用ルールは、この AGENTS.md に追記し、後続の開発作業から参照できる状態にすること。
13. 実行頻度、時間間隔、遅延時間、タイムアウト、件数・サイズ上限、再試行回数、キャッシュ・バッファ・流量制御・表示更新等、運用・性能・シミュレーション挙動を調整する数値は、原則としてソースコードへ固定せず、各コンポーネントが所有する外部Configから変更可能にすること。数学的定数等は対象外とする。
14. すべての要件定義・設計では将来的なアドオン対応を見据え、標準実装を不要に複雑化せず追加・差し替え・拡張できる余地を残すこと。ただし未承認の具体的アドオンAPI等を確定せず、現在の標準要件・性能・決定論・コンポーネント独立性を犠牲にする過剰な抽象化も行わないこと。
15. 対話ベースの要件定義では、関連する質疑を可能な範囲で数件まとめ、各質問に複数の具体的な選択肢を提示すること。選択肢外のカスタム指定も可能とし、未選択内容を確定事項として扱わないこと。

## 進捗報告ルール

1. 実装・検証・設計確定・Issue整理など、継続的な開発作業を行う場合は、作業途中の主要な節目と最終報告で必ず進捗を報告すること。
2. 進捗報告には必ず `作業単位進捗: XX%` と `全体進捗: XX%` の2つを含めること。作業単位進捗は現在取り組んでいる明確な作業単位に対する進捗、全体進捗は現在追跡しているリリース・Phase・Stage等の全体目標に対する進捗とする。
3. 進捗率は、実装量だけでなく、正本設計、production接続、Ref closure、Snapshot/recovery、CI、release evidence等、その作業の完了条件を基準に評価すること。smokeやfixtureだけが通った状態をproduction完了として水増ししないこと。
4. 進捗率を変更した場合は、何が完了したため上がったのか、または何が判明したため据え置き・後退したのかを報告すること。根拠なく進捗率だけを変更しないこと。
5. CIが実行中の場合は「実行中」と明示し、全workflowが完了する前に `9/9 green` 等の完了表現を使用しないこと。失敗がある場合は件数と原因を確認し、修正前に成功扱いしないこと。
6. 各進捗報告では、単に「何をしたか」だけでなく、**何のための作業なのか**を説明すること。現在の作業が上位目標・Issue・blocker・release条件のどれを満たすためのものかを、専門外の読者にも追える日本語で示すこと。
7. 各進捗報告では、**どういう方針・手順で進めているのか**も説明すること。少なくとも、現在の入力となるauthority/正本、実装する境界、検証方法、完了判定の順序が分かるようにすること。単なるファイル名やcommit列挙だけで済ませないこと。
8. 作業開始時または作業単位が切り替わった時は、原則として次の4点を短く示すこと: **目的 / 現在地 / 進め方 / 完了条件**。長時間作業では、方針が変わった時にも更新すること。
9. blockerを解除する場合は、解除対象、解除根拠、残る依存関係を明示すること。内部blockerを解除しただけで親world blocker等まで自動的に解除しないこと。
10. 全体進捗が90%を超えた後は、実作業とGitHub Issuesの内容が乖離しないよう、主要な作業単位またはblockerの状態が変わるたびに関連Issueを確認・整理・更新すること。Issue本文を現在状態の正本として維持し、古い完了項目を「残り」に放置しないこと。
11. Issueおよび進捗報告は日本語を基準とし、API名、クラス名、メソッド名、規格名、識別子、Snapshot、Ref、TileScope等、英語表記が必要または自然な専門用語以外は日本語で記述すること。一般的な説明を不必要に英語で書かないこと。
12. Issue上のチェックリストと実際の作業順を可能な限り1対1で対応させること。実装順を変更した場合は、Issue側の「現在」「次」「残り」も追従して更新すること。
13. 最終報告では、最新head、CI状況、今回完了した内容、現在のblocker/残作業、次に行う作業を簡潔にまとめること。該当しない項目を形式だけのために捏造しないこと。

## ブランチ運用ルール

### 常設ブランチ

- `main`: 安定版・リリース可能な状態。通常の実装作業を直接行わない。
- `develop`: 全コンポーネントと共通文書を統合した次期版。通常の実装・修正を直接行わない。
- `simulation`: シミュレーションコアの統合ブランチ。
- `gateway`: ゲートウェイの統合ブランチ。
- `view`: 一般ビューの統合ブランチ。
- `administration-view`: 管理ビューの統合ブランチ。
- `documentation`: 共通ドキュメント・設計文書・Protocol文書の統合ブランチ。

ブランチ名は原則として小文字を使用し、複数語はハイフンで区切る。

### 通常の実装・修正フロー

1. 機能追加、修正、リファクタリング等は対象コンポーネントの常設ブランチから作業ブランチを作成して行う。
2. 完了した変更は対象コンポーネントの常設ブランチへ Pull Request で統合する。
3. コンポーネント単位で `develop` へ統合可能になった変更は、対象コンポーネントブランチから `develop` へ Pull Request で統合する。
4. 共通ドキュメント変更は `documentation` から作業ブランチを作り、`documentation`、`develop` の順に Pull Request で統合する。
5. `develop` がリリース可能になった場合、`develop` から `main` へ Pull Request で統合する。
6. 常設ブランチへ通常の実装・修正・ドキュメント編集を直接コミットしない。

```text
main
  ↑
develop
  ↑
responsibility branch
  ↑
work branch
```

`responsibility branch` は `simulation`、`gateway`、`view`、`administration-view`、`documentation` のいずれかを指す。

### ドキュメント編集フロー

1. `README`、`CONTRIBUTING.md`、`SUPPORT.md`、`docs/architecture/`、`docs/protocols/` 等は原則 `documentation` の責任分野とする。
2. 共通ドキュメントは `documentation` 基準の `docs/` 作業ブランチで編集し、Pull Requestで `documentation`、次に `develop` へ統合する。
3. 各コンポーネント固有コードと不可分な文書でも、責任分野をまたぐ修正を無断で同一作業ブランチへ含めない。
4. `.github/workflows/` はCI / Workflow資産として扱い、CI / Workflow運用ルールを優先する。

### 別責任分野への修正依頼

1. 現在の作業ブランチとは別の常設ブランチが責任を持つ変更が必要な場合、原則として現在の作業ブランチから直接変更しない。
2. GitHub Issueを作成し、理由、期待変更、影響範囲、関連文書・Protocol・Issue・Pull Requestを記載して対象責任分野へ依頼する。
3. 対応は対象責任分野の常設ブランチから独立作業ブランチを作り、その常設ブランチへPull Requestで統合する。
4. 複数責任分野へ影響する場合もIssueを連携点として分離する。
5. Protocolや正本設計文書の変更は実装側で先行せず、`documentation` 側で正本を更新して `develop` へ統合した後に追従する。
6. 責任分野をまたぐ例外作業は事前にユーザーの明示承認を得る。

### 作業ブランチ命名

目的が判別できる接頭辞を付け、必要以上に責任分野をまたがない。

例: `feature/simulation-world-time`、`fix/gateway-master-reselection`、`refactor/simulation-threading`、`docs/core-concurrency`。

### プロトコル変更

コンポーネント間Protocolを変更する場合は実装より先に正本設計書を更新する。変更理由・影響範囲をIssue化し、`documentation` 基準の専用作業ブランチで `docs/protocols` を更新し、`documentation` → `develop` の順に統合する。各コンポーネントは更新済み正本へ独立して追従し、複数コンポーネント実装を1つの共有作業ブランチへまとめない。

### 同期と独立性

- 各コンポーネントブランチと `documentation` を `develop` から長期間乖離させない。
- 具体的なmerge/rebase頻度は固定しない。
- コンポーネント完全分離を維持し、別コンポーネントの内部実装を通信契約として参照しない。
- 複数コンポーネント間契約はProtocol設計書を介して連携する。
- コンポーネント側と `documentation` 側が互いの責任分野を通常フローで直接編集しない。

## CI / Workflow 運用ルール

### 基本方針

1. GitHub Actions の Workflow、CI、横断自動化はリポジトリ共通の運用資産とする。
2. Workflow は原則 `.github/workflows/` で管理し、コードレビュー可能にする。
3. CIを通すためだけにコンポーネント境界、Protocol契約、決定論要件、設計原則を崩さない。
4. 実装対象・ビルド・テスト方法が未確定の状態で仮定に基づくCIを先回りで作らない。

### Workflow変更用ブランチ

1. 共通CI / Workflow変更は原則 `develop` から `ci/` 接頭辞の専用作業ブランチを作成する。
2. `develop` への Pull Request で統合し、常設ブランチへ直接コミットしない。
3. 特定コンポーネント用CIでも `.github/workflows` 自体は共通領域として扱う。

### コンポーネント独立性

1. Simulation、Gateway、View、AdministrationView のビルド・テストは可能な限り独立CI単位とする。
2. 他コンポーネントの実装コード、DLL、内部型、共有DTOライブラリを必要とする構成を作らない。
3. コンポーネント間通信テストはProtocol設計書に基づくスタブ、モック、テストPeer等を優先する。
4. 無関係な高コストCIを無条件に実行しない。path filter等は実装構成確定後に定義する。
5. 全体統合検証は単体CIとは別Workflowとして責務を明示する。

### Pull Request と必須チェック

1. PR向けCIは原則としてビルド、テスト、静的検査等の検証に限定し、デプロイ等の破壊的副作用を行わない。
2. 必須チェックは実在し、安定し、統合可否判断に必要なものだけ登録する。
3. 未完成・不安定・存在しないCIを先に必須化しない。
4. Workflow/Job名は目的と対象が判別できる名称にする。
5. CIを一時無効化してマージ制約を回避することを通常手段にしない。

### 権限とセキュリティ

1. `GITHUB_TOKEN` は最小権限とし、必要な `permissions` だけを明示する。
2. 不要なwrite権限を検証Workflowへ付与しない。
3. 秘密情報をWorkflow、ソース、ログ、Artifactへ直接記録しない。
4. 秘密情報はGitHub Secrets等の適切な管理機構を使う。
5. Fork由来PRや信頼できない入力から秘密情報へ到達できるWorkflowを作らない。
6. `pull_request_target` 等は明確な必要性とセキュリティ設計なしに使用しない。
7. PRタイトル、ブランチ名、Issue本文、コミットメッセージ等の外部入力をShellコマンドへ直接展開しない。

### 外部Actionと依存関係

1. 外部Action導入時は提供元、用途、必要権限を確認する。
2. 外部Actionは再現可能性とサプライチェーン安全性を考慮し、不変なcommit SHAへ固定することを原則とする。
3. バージョン更新は通常の依存関係更新としてレビューする。
4. 不要なActionや過剰な外部依存を導入しない。

### デプロイ・リリースWorkflow

1. デプロイ、Release、Package公開等は通常PR検証Workflowと分離する。
2. 対象、実行元、承認、Environment、権限、失敗時扱いが設計されるまで作成しない。
3. `main` 統合だけを根拠に未承認の配布先へ自動配布しない。
4. 破壊的・公開操作には誤実行防止の明示条件を設ける。

### Workflow品質

1. 同じ入力に対する再現可能性を優先する。
2. Job責務を明確にし、異なる責務を理由なく巨大Jobへ混在させない。
3. Artifactの目的・保持対象・秘密情報非包含を確認する。
4. キャッシュ有無で検証結果の正否が変わらない構成にする。
5. CI時間短縮だけを理由に必要な検証を無断で省略しない。

## 言語ルール

1. このリポジトリは日本語を基準言語とする。
2. README、設計書、仕様書、運用手順、開発ガイド、ADR、Issueテンプレート、Pull Requestテンプレート等のドキュメントは原則日本語で作成する。
3. 説明文、見出し、注釈も原則日本語とする。
4. 技術用語、API名、クラス名、メソッド名、ライブラリ名、規格名など英語表記が自然または必要なものは無理に日本語化しない。
5. 英語版ドキュメントはユーザーから明示的な指示がある場合に限る。

## 優先順位

提案または実行しようとしている作業がこのルールと競合する場合は、実装前に停止し、ユーザーから明示的な実装指示または方針変更があるまで、議論・要件整理・計画に戻ること。
